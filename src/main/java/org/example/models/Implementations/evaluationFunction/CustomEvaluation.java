package org.example.models.Implementations.evaluationFunction;

import org.example.models.interfaces.EvaluationFunction;

public class CustomEvaluation implements EvaluationFunction {
  @Override
  public double evaluate(double[] values) {
    /*
    Implement the function that will evaluate the values (the function that we are optimizing) here.
    The function will evaluate the values and return said evaluation.
    */
    throw new UnsupportedOperationException(
        "evaluate function in CustomEvaluation object is not implemented yet.");
  }
}
